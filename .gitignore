# Python
__pycache__/
*.pyc
.venv/
venv/

# secrets / local
.env
*.local

# phase execution outputs
phases/**/phase*-output.json
phases/**/step*-output.json

# NOTE: data/jobs.json은 일부러 ignore하지 않는다 — raw URL "API" 역할이므로 커밋한다.
